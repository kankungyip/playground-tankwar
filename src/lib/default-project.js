import { Text } from '@blockcode/core';

export const defaultProject = {
  files: [
    {
      id: 'player',
      name: (
        <Text
          id="tankwar.stageInfo.player"
          defaultMessage="Player"
        />
      ),
    },
  ],
};
